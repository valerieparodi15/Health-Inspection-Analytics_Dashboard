-- View used to create big_company_violations

CREATE VIEW company_location_counts AS
SELECT
parent_company
COUNT(*) as location_counts
FROM inspections_clean
GROUP BY parent_company
HAVING COUNT(*)>10;
